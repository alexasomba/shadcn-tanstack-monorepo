# Generate: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-with-32-plus-char-secret
# Public origin of this app (auth handler base URL)
BETTER_AUTH_URL=http://127.0.0.1:8301
# Optional comma-separated CSRF allowlist (defaults include monorepo dev ports)
# BETTER_AUTH_TRUSTED_ORIGINS=http://127.0.0.1:8300,http://127.0.0.1:8301
# Client override only if auth API is on another host
# VITE_BETTER_AUTH_URL=http://127.0.0.1:8301

# --- Transactional email (better-notify → OneSignal) ---
# Required for live verify/reset/invite/OTP from this Worker. Secrets → .dev.vars / wrangler secret.
ONESIGNAL_APP_ID=
ONESIGNAL_API_KEY=
# NOTIFY_DRY_RUN=1

# Paystack (optional)
# PAYSTACK_SECRET_KEY=sk_test_...
# PAYSTACK_WEBHOOK_SECRET=sk_test_...
